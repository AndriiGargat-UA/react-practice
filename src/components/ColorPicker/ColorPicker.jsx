import { Component } from "react";

export class ColorPicker extends Component {
    render() {
        return (
            <div>
                <h2>COLOR PICKER</h2>
                <div>
                    {options.map(({ label, color }) => (
                        <span
                        key=></span>
                    ))}
                </div>
            </div>
        )
    }
}
